# 无限画布 · 技术方案书

> **文档版本：** v1.0
> **编写日期：** 2026-07-29
> **项目状态：** 规划阶段
> **基座项目：** infinite-canvas v0.4.5（AGPL-3.0）
> **技术栈：** Go + Gin + GORM / Next.js + React + TypeScript + Ant Design + Tailwind

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术选型总表](#2-技术选型总表)
3. [系统架构总览](#3-系统架构总览)
4. [模块一 · 素材库](#4-模块一--素材库)
5. [模块二 · 创作工作台](#5-模块二--创作工作台)
6. [模块三 · 工作流体系](#6-模块三--工作流体系)
7. [模块四 · 提示词库](#7-模块四--提示词库)
8. [前端页面规划](#8-前端页面规划)
9. [演进路线](#9-演进路线)
10. [技术风险与可替换性](#10-技术风险与可替换性)

---

## 1. 项目概述

### 1.1 项目定位

无限画布是一款面向图片、视频、音频全能创作的开源工作台。它把画布编排、AI 生图、视频生成、提示词库和素材管理放在同一个界面里，适合用来探索视觉方案并连续迭代创作结果。

### 1.2 优化目标

整套系统划分为**素材库、创作工作台、提示词库、工作流**四大核心层级，每个模块都配套对应的功能优化点，形成完整的 AI 创作闭环。

### 1.3 设计原则

1. **最小侵入**：基于现有 Go + Next.js 架构增量改造，不推翻重来
2. **可替换性**：每个技术选型预留替代方案，避免供应商锁定
3. **渐进交付**：模块独立上线，不阻塞已有功能
4. **前端简洁**：比当前版本更易操作，降低认知负荷

---

## 2. 技术选型总表

| 层级 | 模块 | 当前状态 | 推荐选型 | 前沿性 | 替代方案 |
|------|------|----------|----------|--------|----------|
| 数据 | 向量检索 | ❌ 无 | Qdrant（自托管） / pgvector | ✅ 前沿 | Milvus（分布式）、Weaviate（GraphQL）、OpenSearch k-NN |
| 数据 | 对象存储 | 本地文件系统 | MinIO（S3 兼容） | ✅ 当前推荐 | AWS S3、阿里云 OSS、Cloudflare R2 |
| AI | 图像理解 | ❌ 无 | CLIP + DeepDanbooru | ✅ 前沿 | Qwen-VL、GPT-4V、Gemini Vision |
| AI | 视频理解 | ❌ 无 | VideoCLIP / TimeSformer | ✅ 前沿 | Gemini 1.5 Pro（长上下文视频）、Twelve Labs API |
| AI | 反推提示词 | ❌ 无 | WD14-Tagger + CLIP Interrogator | ✅ 前沿 | JoyCaption、CogVLM2 |
| AI | 人物一致性 | ❌ 无 | IP-Adapter-FaceID + InstantID | ✅ 前沿 | ReActor、FaceFusion、PhotoMaker |
| AI | Agent 框架 | ❌ 无 | 自研（Go Function Calling） / Dify | ✅ 前沿 | LangChain、CrewAI、AutoGen |
| 画布 | 自动布局 | ❌ 无 | Dagre（有向图） / ELK（Eclipse） | ✅ 成熟 | Cytoscape.js 布局、d3-hierarchy |
| 工作流 | 引擎 | ❌ 无 | Temporal（Go SDK）/ Prefect | ✅ 前沿 | Argo Workflows、BullMQ（轻量）、自研 DAG |
| 前端 | 拖拽 | React Flow（已有） | 升级 React Flow v12 + @dnd-kit | ✅ 前沿 | xyflow（Svelte 版）、Vue Flow |
| 前端 | 搜索 | Ant Design Input | Fuse.js（前端模糊搜索）+ 后端全文索引 | ✅ 成熟 | Lunr.js、MeiliSearch、Typesense |
| 部署 | 容器化 | ❌ 手动 | Docker Compose（已有配置待修复） | ✅ 成熟 | K8s、Podman、Kamal |

> **可替换性原则**：每个选型下方均标注 2-3 个替代方案，若主流技术发生变更（如 Qdrant 停止维护、CLIP 被新模型超越），可在不改变系统架构的前提下切换。选型标准以"开源优先、社区活跃度 >5000 stars、最近 6 个月有发版"为准。若未来出现更优方案（如 GPT-5 视觉能力碾压 CLIP），通过适配器模式解耦 AI 服务接口，切换成本控制在 2-3 个工作日内。

---

## 3. 系统架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (Next.js 16)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐ │
│  │  画布     │  │ 生图工作台│  │ 视频创作台│  │  提示词中心  │ │
│  │ React    │  │ 参数面板 │  │ 参数面板 │  │  分类+搜索   │ │
│  │ Flow v12 │  │ 模板引擎 │  │ 拖拽排序 │  │  收藏+分组   │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──────┬──────┘ │
│       │              │              │               │        │
│  ┌────┴──────────────┴──────────────┴───────────────┴────┐  │
│  │                    共享组件层                           │  │
│  │  提示词智能生成 · 素材选择器 · AI Agent 对话面板        │  │
│  └────────────────────────┬───────────────────────────────┘  │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                          ▼                   后端 (Go + Gin) │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                    AI Agent 引擎                       │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐ │  │
│  │  │ 导演Agent │  │ 编剧Agent │  │ 工作流编排引擎        │ │  │
│  │  │ 运镜/站位 │  │ 剧本/分镜 │  │ Temporal / DAG      │ │  │
│  │  └──────────┘  └──────────┘  └──────────────────────┘ │  │
│  └──────────────────────────┬────────────────────────────┘  │
│                              │                                │
│  ┌──────────────────────────┼────────────────────────────┐  │
│  │          ▼               ▼               ▼            │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐      │  │
│  │  │ 素材服务    │  │ 创作服务    │  │ AI 服务    │      │  │
│  │  │ 批量导入    │  │ 画布编排    │  │ CLIP 反推  │      │  │
│  │  │ 语义检索    │  │ 工作流引擎  │  │ Agent 编排 │      │  │
│  │  │ 模板管理    │  │ 自动回填    │  │ RAG 知识库 │      │  │
│  │  └──────┬─────┘  └──────┬─────┘  └──────┬─────┘      │  │
│  └─────────┼───────────────┼───────────────┼─────────────┘  │
│            ▼               ▼               ▼                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  数据层: SQLite(本地) / MySQL(生产) + Qdrant + MinIO  │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

---

## 4. 模块一 · 素材库

> **核心作用：** 素材收纳与分类管理，内部划分为文本、图片、视频、音频存储类目。

### 4.1 现存痛点

> 现有素材库只支持作者自己导入、导出素材，并不提供素材和模板。导入素材不能批量操作。分类基于标签"文本""图片""视频"。

### 4.2 改进方案

#### 4.2.1 向量语义检索

| 项目 | 说明 |
|------|------|
| 目标 | 用户可用自然语言描述检索素材（如"暖色调的日落风景图"） |
| 技术 | Qdrant 向量数据库 + CLIP Embedding（图像）/ text-embedding-3-small（文本） |
| 流程 | 素材入库 → 自动生成 Embedding → 存入 Qdrant → 前端自然语言搜索 → 返回相似 Top-K |
| 替代 | 若 Qdrant 停止维护 → 切换 pgvector（PostgreSQL 扩展）；若 CLIP 被超越 → 适配器层替换为新 Vision Encoder |

#### 4.2.2 批量导入

| 项目 | 说明 |
|------|------|
| 目标 | 支持拖拽文件夹/多选文件批量导入，自动提取元数据 |
| 技术 | 前端：File API + 拖拽区域 + 进度条；后端：Go 并发管道（goroutine pool）处理上传 + ffprobe 提取视频/音频元数据 |
| 细节 | 支持剧本(.txt/.md)、配图(.png/.jpg/.webp)、音频(.mp3/.wav)、全套工作流模板(.json) 一次性打包导入 |
| 替代 | 大文件场景可接入 tus 协议（断点续传），或直接对接 MinIO presigned URL 绕过服务端 |

#### 4.2.3 素材归类体系重构

| 项目 | 说明 |
|------|------|
| 目标 | 区分用途（剧本/配音/参考图/音效）、风格（写实/二次元/赛博朋克）、来源（自上传/模板库/社区），多维标签体系 |
| 技术 | 自定义标签系统（Tag）替代单一类型枚举；标签支持层级（父标签/子标签）和颜色标记；后端 GORM 多对多关联 |
| 替代 | 标签体系基于 PostgreSQL ltree 或 Closure Table 均可迁移，不绑定具体实现 |

#### 4.2.4 素材跨画布调用

| 项目 | 说明 |
|------|------|
| 目标 | 用户在画布 A 上传的素材，在画布 B 中可直接搜索调取，无需重新上传 |
| 技术 | 全局素材池（global asset pool）+ 画布级引用关系；素材实体与画布项目解耦，画布仅存引用 ID |
| 替代 | 若未来需要跨用户共享，引入 MinIO bucket policy 做租户隔离即可 |

### 4.3 成套素材模块

> 分为合集与模板两类内容，支持批量导入整套创作物料。

#### 4.3.1 模板复用引擎

| 项目 | 说明 |
|------|------|
| 目标 | 用户保存自定义短剧、海报、分镜流水线模板后，新建工程可一键载入整套流程，仅替换核心文案/素材即可复用 |
| 技术 | JSON Schema 定义模板格式（含节点、连线、提示词、素材引用）；模板导入时解析 JSON → 新画布实例化 → 替换素材 UUID 映射表 |
| 替代 | 模板格式可用 YAML 替代 JSON（可读性更好）；若需跨平台兼容，可转换为 ComfyUI workflow JSON 格式 |

#### 4.3.2 模板推荐系统

| 项目 | 说明 |
|------|------|
| 目标 | 用户上传素材，收藏/点赞量高的自动列为推荐模板 |
| 技术 | Redis Sorted Set 做热度排名（收藏+1、点赞+1、使用+2 加权）；定时任务（cron）将热门模板同步到首页推荐位 |
| 替代 | 若需更精细推荐，可升级为协同过滤（user-based CF）或嵌入向量相似度推荐，但初期 Redis 排名足够 |

---

## 5. 模块二 · 创作工作台

> 包含画布、生图工作台、视频创作台三大创作载体。画布作为总统筹工作区，承载全流程节点编排；生图工作台专注画面绘制调参；视频创作台负责动态镜头、成片剪辑合成。

### 5.1 画布

#### 5.1.1 现存痛点

> 画布的目标是节点不同作用可以用不同颜色；节点可以自由拖拽到其他框；画布的功能键参考图这里可以用 AI 生成黄金参考图，提示词和反推提示词提供模板。可以选择一键横向/纵向排版。

#### 5.1.2 节点颜色映射

| 项目 | 说明 |
|------|------|
| 目标 | 生图节点、视频节点、提示词节点、素材节点等不同类型用不同颜色区分，一目了然 |
| 技术 | React Flow 节点类型 → 颜色主题映射表；用户可在设置中自定义颜色方案；预设 6 色：🟦生图/🟩视频/🟨提示词/🟪素材/🟧脚本/⬜其他 |
| 替代 | 颜色方案基于 CSS 变量实现，换 UI 框架（如 Vue Flow）时仅需修改变量映射 |

#### 5.1.3 AI 黄金参考图

| 项目 | 说明 |
|------|------|
| 目标 | 在画布中选中提示词节点，一键生成"黄金参考图"作为构图参考 |
| 技术 | 提示词 → ControlNet（Canny/Depth/Scribble）生成结构参考图；前端展示在节点侧面板中，可拖拽到画布作为新节点 |
| 替代 | 若 ControlNet 被新方法超越（如 T2I-Adapter、IP-Adapter 结构控制），适配器层切换即可 |

#### 5.1.4 自动排版

| 项目 | 说明 |
|------|------|
| 目标 | 一键横向/纵向自动排版，降低多节点排布操作成本 |
| 技术 | Dagre 布局算法（有向无环图自动分层）；横向（LR）/纵向（TB）一键切换；支持自定义节点间距和层间距 |
| 替代 | ELK（Eclipse Layout Kernel）支持更复杂约束（端口约束、子图嵌套），可平滑升级 |

#### 5.1.5 工作台双向跳转与自动回填

| 项目 | 说明 |
|------|------|
| 目标 | 在画布内可一键跳转至生图工作台/视频创作台精细修改，工作台调整后的内容自动回传同步至画布节点 |
| 技术 | URL query parameter 传递来源画布 ID 和节点 ID；工作台"保存"时回调 API → 更新画布对应节点；WebSocket 推送画布实时刷新 |
| 替代 | 若 WebSocket 过重，可用 SSE（Server-Sent Events）替代推送通知 |

### 5.2 生图工作台

#### 5.2.1 现存痛点

> 生图工作台缺少提供提示词。

#### 5.2.2 智能提示词生成

| 项目 | 说明 |
|------|------|
| 目标 | 上传参考图后，AI 自动识别图像内容并生成提示词 |
| 技术 | 图片 → CLIP Interrogator / WD14-Tagger → 输出标签（性别、年龄、肤色、头发、服饰、场景、光线、风格）；标签自动填入提示词输入框，用户可直接使用或修改 |
| 替代 | Qwen-VL-Max / GPT-4V 提供更细腻的自然语言描述，可作为升级路径 |

#### 5.2.3 提示词模板引擎

| 项目 | 说明 |
|------|------|
| 目标 | 提供预设提示词模板（电商海报、人物写真、风景壁纸等场景），用户选择模板后仅修改关键参数 |
| 技术 | 模板语法：`{{subject}}在{{scene}}中，{{lighting}}光线，{{style}}风格` → 用户填写 4 个变量 → 生成完整提示词；模板存储在提示词库中，生图工作台通过 API 拉取 |
| 替代 | 模板引擎使用 Go template 标准库，零外部依赖，迁移成本为零 |

### 5.3 视频创作台

#### 5.3.1 现存痛点

> 视频创作台缺少提供提示词，上传的参考图只能切换到上一张、下一张，不能直接拖拽到最前面、最后面。

#### 5.3.2 参考图拖拽排序

| 项目 | 说明 |
|------|------|
| 目标 | 参考图支持拖拽排序，可自由调整顺序（拖到最前/最后/指定位置） |
| 技术 | @dnd-kit（React 拖拽库，轻量、可访问性好）替换当前仅支持前后翻页的简单轮播；排序结果保存到后端 |
| 替代 | react-beautiful-dnd（更成熟，但维护已停止）→ 切换为 @dnd-kit 是正确的；未来若迁移至 Vue 可用 vuedraggable |

#### 5.3.3 首帧/尾帧上传

| 项目 | 说明 |
|------|------|
| 目标 | 上传首帧和尾帧图片，控制视频起始和结束画面，保持人物/场景一致性 |
| 技术 | 前后端各增一个上传位；生成请求时将首帧/尾帧作为 ControlNet 输入传递给视频生成 API；IP-Adapter-FaceID 锁定人物面部一致性 |
| 替代 | InstantID 或 PhotoMaker 可作为 IP-Adapter 的替代方案，通过适配器层注入 |

#### 5.3.4 视频智能提示词

| 项目 | 说明 |
|------|------|
| 目标 | 上传参考视频后，AI 识别转场、运镜、风格并生成提示词，甚至给出滤镜参数 |
| 技术 | 视频 → 关键帧提取（FFmpeg 每秒 1 帧）→ CLIP/ViViT 逐帧分析 → 汇总输出：运镜类型（推拉摇移）、转场风格（淡入/硬切/滑动）、色调方案 |
| 替代 | Twelve Labs API（专做视频理解，但闭源付费）→ 自研方案作为默认，API 作为可选增强 |

### 5.4 导演台

> 目标：可以上传参考图或参考视频，利用 AI 识别机位角度和参数，生成类似图片/视频的角度；根据剧本/文字推荐参数；或者生成导演 Agent。

#### 5.4.1 导演 Agent

| 项目 | 说明 |
|------|------|
| 目标 | 具备专业导演知识的 AI Agent，懂得运镜、站位、构图等专业知识 |
| 技术 | RAG 知识库（导演学教材、电影镜头语言数据）+ LLM Function Calling；输入剧本段落 → 输出机位图（俯视图标注）、镜头参数（焦距/光圈/角度）、运镜建议 |
| 替代 | 知识库可接入 LangChain/LlamaIndex，或使用 Dify 低代码搭建，不锁定具体框架 |
| 知识源 | 初始加载《电影镜头设计》《导演的摄影课》等公开教材摘要 + 经典电影分镜案例 |

#### 5.4.2 编剧 Agent

| 项目 | 说明 |
|------|------|
| 目标 | 根据主题自动生成分镜头脚本，输出可直接用于生图/生视频的提示词序列 |
| 技术 | LLM + 结构化输出（JSON Schema）：主题 → 场景列表 → 每场景镜头描述 + 提示词 + 时长；输出结果自动拆分为画布节点 |
| 替代 | 任意支持 JSON Mode 的 LLM 均可替换（GPT-4o、Claude、DeepSeek-V3） |

---

## 6. 模块三 · 工作流体系

> 完整链路为记录创作操作、形成多步骤执行流程、汇总生成专属执行词。

### 6.1 现存痛点

> 目前需要手动输入产品名称、核心卖点、活动信息，生成提示词预览，或者上传参考图由 AI 生成。希望最终能交由导演 Agent / 助手 Agent 执行整套创作指令。

### 6.2 操作录制与回放

| 项目 | 说明 |
|------|------|
| 目标 | 完整记录用户每一步节点连线、参数修改、AI 执行操作，形成可复盘、可保存的专属工作流 |
| 技术 | Event Sourcing 模式：每个操作记录为不可变事件（NodeCreated, EdgeAdded, ParamChanged, AIGenerated）；事件流存储在 SQLite/MySQL 中；回放时按时间戳顺序重放事件流 |
| 替代 | 若需大规模事件流处理，可切换至 Kafka/Redpanda 做事件总线；初期 SQLite 足以 |

### 6.3 智能执行词生成

| 项目 | 说明 |
|------|------|
| 目标 | 自动整合整条链路的提示词、素材引用、执行指令，AI 可完整复刻整套创作逻辑 |
| 技术 | 工作流 DAG 序列化 → 提取所有 Prompt 节点 + 参数 + 素材引用 → LLM 聚合生成「执行词」→ 可导出为独立 JSON/文本，供新工程一键执行 |
| 替代 | 执行词格式可扩展为 ComfyUI API JSON 格式，与现有 AI 工作流生态互通 |

### 6.4 流程快照与局部重跑

| 项目 | 说明 |
|------|------|
| 目标 | 修改单一节点仅触发下游相关步骤重新执行，无需整条工作流从头运行 |
| 技术 | DAG 依赖分析 → 修改节点 → BFS 查找所有下游节点 → 仅重跑受影响的下游链路；快照功能：每个节点执行后保存输入/输出状态，支持任意节点回退 |
| 替代 | Temporal（Go SDK）原生支持 Workflow 重放和局部重试，可作为升级路径；Prefect（Python）是轻量替代 |

### 6.5 主题自动编排

| 项目 | 说明 |
|------|------|
| 目标 | 输入主题（如"电商海报生成""小红书文章配图"），自动生成核心卖点、活动信息、提示词预览 |
| 技术 | LLM 接收主题 → 结构化输出：{ title, sellingPoints[], activityInfo, promptPreview, suggestedModel, suggestedParams } → 前端渲染为可编辑表单 |
| 替代 | 预设场景模板（Template）作为 LLM 的 fallback：网络不可用时仍可使用本地模板 |

---

## 7. 模块四 · 提示词库

> 用 AI 能力提供涵盖固定保存的基础提示词、即时自定义调整的临时提示词。

### 7.1 现存痛点

> 目前只有用户提供的提示词。上传图片无法直接绑定、联动对应提示词。

### 7.2 素材-提示词绑定

| 项目 | 说明 |
|------|------|
| 目标 | 图片、剧本等素材可挂载专属正向/反向提示词，复用创作参数 |
| 技术 | 数据库：`asset_prompts` 关联表（asset_id, prompt_id, type[positive/negative], order）；素材详情页展示关联提示词，支持一键添加到当前工作台 |
| 替代 | 关联表设计通用，任何关系型/非关系型数据库均可实现 |

### 7.3 全局/私有双存储

| 项目 | 说明 |
|------|------|
| 目标 | 区分全局公用提示词和单工程私有提示词，支持分组、批量导入导出 |
| 技术 | prompts 表增加 scope 字段（global/private）+ project_id（scope=private 时关联）；分组使用 category 字段（可多选）；批量导入导出为 JSON/CSV |
| 替代 | 权限控制未来可接入 RBAC（角色-权限），当前 scope 字段已预留扩展 |

### 7.4 收藏夹与快速调用

| 项目 | 说明 |
|------|------|
| 目标 | 提示词可以收藏并分类，分为"经常使用"和"收藏"。收藏包括图片、文本、视频、导演台，可在各个层级里面随时调用 |
| 技术 | 用户-提示词收藏关联表 + Redis 缓存最常使用 Top 20；前端全局"提示词快捷面板"（侧边栏/浮动按钮），不区分当前处于哪个工作台，随时呼出 |
| 替代 | 收藏逻辑简单，任何 KV 存储（Redis/本地 localStorage fallback）均可承载 |

### 7.5 全应用通用智能生成

| 项目 | 说明 |
|------|------|
| 目标 | 在生图工作台、视频创作台、画布等任何位置，根据参考内容自动生成提示词 |
| 技术 | 统一的 `PromptGenerator` 服务（Go 后端）：接收素材 ID → 调用对应 Vision 模型 → 返回结构化提示词 → 各工作台通过 API 调用，保证生成逻辑一致 |
| 替代 | Vision 模型通过接口解耦（`type VisionAnalyzer interface { Analyze(media) Prompt }`），切换模型不影响调用方 |

---

## 8. 前端页面规划

> 希望简洁明了，甚至比之前更容易操作。

### 8.1 布局原则

| 原则 | 说明 |
|------|------|
| 统一侧边栏 | 左侧固定导航（画布/生图/视频/提示词/素材），减少顶栏+侧边栏双重导航的认知负担 |
| 工作台切换 | 画布中嵌入生图/视频工作台作为可展开面板，无需跳转页面 |
| 快捷操作 | 全局悬浮按钮：提示词面板、素材选择器、AI 助手对话 |
| 拖拽优先 | 素材到节点、节点到节点、参考图排序均支持拖拽 |

### 8.2 页面结构树

```
├── /                   首页（保留当前简洁风格）
├── /canvas             画布（总统筹工作区）
│   ├── [侧面板] 生图工作台（可展开/收起）
│   ├── [侧面板] 视频创作台（可展开/收起）
│   └── [工具栏] 自动排版 / 导出 / 模板保存
├── /prompts            提示词中心
│   ├── 全局提示词 Tab
│   ├── 我的收藏 Tab
│   └── 分组管理
├── /assets             素材库
│   ├── 我的素材
│   ├── 模板市场
│   └── 合集管理
└── /settings           设置
    ├── API 配置
    ├── 主题/颜色方案
    └── 模板管理
```

### 8.3 交互优化

| 优化点 | 当前 | 改进 |
|--------|------|------|
| 参考图排序 | 仅前后翻页按钮 | 拖拽排序 + 缩略图网格 |
| 提示词输入 | 纯文本框 | 模板选择器 + 变量表单 + AI 生成按钮 |
| 素材选择 | 页面跳转到素材库 | 任何位置呼出素材选择器面板 |
| 生成历史 | 仅列表展示 | 缩略图网格 + 一键回填到画布 |

---

## 9. 演进路线

### Phase 1：基础补齐（3 周）

| 序号 | 任务 | 产出物 |
|------|------|--------|
| 1 | 素材批量导入 + 标签体系重构 | 拖拽上传、多维标签 CRUD |
| 2 | 生图/视频工作台提示词模板 | 模板引擎 + 10 套预设模板 |
| 3 | 参考图拖拽排序 + 首尾帧上传 | @dnd-kit 集成 + API |
| 4 | 画布节点颜色映射 + Dagre 自动排版 | 6 色主题 + LR/TB 布局 |
| 5 | 提示词收藏 + 全局快捷面板 | Redis 缓存 + 悬浮面板 |
| 6 | 前端页面结构改造 | 侧边栏统一布局 |

### Phase 2：AI 增强（4 周）

| 序号 | 任务 | 产出物 |
|------|------|--------|
| 1 | 向量语义检索（素材库） | Qdrant + CLIP Embedding |
| 2 | 图片反推提示词（CLIP Interrogator） | 生图/视频台一键生成提示词 |
| 3 | 素材-提示词绑定 | 关联表 + API + 前端联动 |
| 4 | 工作流操作录制 | Event Sourcing + 回放 |
| 5 | 画布 ↔ 工作台双向跳转 + 自动回填 | WebSocket + 状态同步 |
| 6 | 模板复用引擎 | JSON 模板定义 + 一键导入 |

### Phase 3：智能体与自动化（5 周）

| 序号 | 任务 | 产出物 |
|------|------|--------|
| 1 | 导演 Agent（RAG + 运镜知识库） | Agent 服务 + 知识库初始化 |
| 2 | 编剧 Agent（分镜脚本生成） | JSON Schema 结构化输出 |
| 3 | 工作流智能执行词生成 | LLM 聚合 + DAG 序列化 |
| 4 | 流程快照 + 局部重跑 | BFS 依赖分析 + 节点级重试 |
| 5 | 主题自动编排（海报/小红书等） | 场景模板 + LLM 结构化输出 |
| 6 | 人物一致性锁定（IP-Adapter） | 首尾帧 + FaceID 管线 |
| 7 | 成套素材模板市场 | 模板上传/审核/推荐系统 |

---

## 10. 技术风险与可替换性

### 10.1 核心原则

> 不假设任何一个技术组件是"世界最好"的。每个关键路径都预留替代方案，架构上通过接口/适配器层解耦，确保技术栈更迭时不会导致系统重构。

### 10.2 关键组件可替换性矩阵

| 组件 | 当前选型 | 替换条件 | 替换方案 | 切换成本 |
|------|----------|----------|----------|----------|
| 向量数据库 | Qdrant | 社区停滞/性能不达标 | pgvector / Milvus / Weaviate | 2-3 天（仅改 Embedding Service 适配器） |
| Vision 模型 | CLIP + WD14 | 新模型准确率显著超越 | Qwen-VL / GPT-4V / Gemini | 1-2 天（实现 VisionAnalyzer 接口） |
| 工作流引擎 | 自研 Event Sourcing | 复杂度过高/需分布式 | Temporal Go SDK / Prefect | 1 周（核心逻辑重写） |
| 自动布局 | Dagre | 需端口约束/子图嵌套 | ELK.js | 1 天（布局函数替换） |
| Agent 框架 | 自研 Function Calling | 维护成本高/功能不足 | Dify / LangChain | 3-5 天（Agent 路由层重写） |
| 拖拽库 | @dnd-kit | 项目迁移至 Vue | vuedraggable / Vue Flow | 取决于 UI 框架切换 |
| 对象存储 | 本地文件系统 | 需多节点共享/容量扩展 | MinIO / S3 / R2 | 1-2 天（Storage Adapter 切换） |
| LLM | OpenAI 兼容 API | 成本/政策变化 | DeepSeek / Qwen / Claude | 0 天（API 兼容层，仅改 endpoint + key） |

### 10.3 未来技术监控清单

> 以下技术目前尚不成熟或不需要，但值得持续关注。若成熟度达标，可快速集成。

| 技术 | 关注原因 | 当前状态 |
|------|----------|----------|
| Sora / Kling / 可灵 | 视频生成质量飞跃，可能改变视频创作台工作流 | 闭源/内测中 |
| GPT-5 / 下一代多模态 | 单一模型同时理解图/视频/音频，可能替代 CLIP+WD14 管线 | 未发布 |
| WebGPU | 浏览器端 AI 推理（如 Web Stable Diffusion），可降低服务端 GPU 成本 | Chrome 113+ 支持，生态待成熟 |
| ComfyUI 生态 | 若画布编排能力被 ComfyUI 超越，可考虑部分对接 | 持续关注 |
| Flux / SD3 | 新一代图像生成模型，生图质量提升 | 部分已可用 |
| MLX (Apple Silicon) | Mac 本地高效推理，可能支持客户端本地 AI 功能 | 需评估用户设备分布 |

### 10.4 降级策略

当 AI 服务不可用（网络/额度/故障）时：

```
AI 功能不可用
  ├─ 提示词生成 → 回退到本地模板 + 手动输入
  ├─ 语义检索 → 回退到传统标签/标题文本搜索
  ├─ Agent 编排 → 回退到手动操作，工作流仍可运行
  ├─ 反推提示词 → 提示用户手动填写
  └─ 黄金参考图 → 功能灰显，标注"AI 不可用"
```

所有 AI 功能设计为"增强"而非"必需"，核心创作流程脱离 AI 依然可完成。

---

> **文档维护：** 本方案书随项目推进持续更新。技术选型每季度审视一次，跟踪替代方案的社区活跃度和版本发版情况。若选型组件 6 个月内无新版本发布，触发重新评估流程。
